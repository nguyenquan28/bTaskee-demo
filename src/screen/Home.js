import React from 'react'
import { SafeAreaView } from 'react-native'

const Home = ({ navigation }) => {
    return (
        <SafeAreaView>
            <Text>
                Home Page
            </Text>
        </SafeAreaView>
    )
}